%YAML 1.1
---
name: ListenerLaunchRunner
spec_version: 1.1
spec_type: interface
description: "This capability implements the listener component of the audio stack and is a dependency for other capabilities
              that use audio stack functionalities."
interface:
  actions:
    "/speech_to_text":
      type: "hri_audio_msgs::action::SpeechToText"
      description: "Speech to text action that listens to the surrounding sounds"
