%YAML 1.1
---
name: SpeakerLaunchRunner
spec_version: 1.1
spec_type: interface
description: "This capability implements the speaker component of the audio stack and is a dependency for other capabilities
              that use audio stack functionalities."
interface:
  actions:
    "/text_to_speech":
      type: "hri_audio_msgs::action::TextToSpeech"
      description: "Text to Speech action that allows speaking the text given"
