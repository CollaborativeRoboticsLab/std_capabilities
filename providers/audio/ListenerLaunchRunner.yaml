# the empty provider
%YAML 1.1
---
name: ListenerLaunchRunner
spec_type: provider
spec_version: 1.1
description: "The capability provider for the audio stack"
implements: std_capabilities/ListenerLaunchRunner
runner: hri_audio_stack/listen.launch.py
