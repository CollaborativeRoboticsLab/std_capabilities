# the empty provider
%YAML 1.1
---
name: SpeakerRunner
spec_type: provider
spec_version: 1.1
description: The capability provider for the text_to_speech interface
implements: std_capabilities2/SpeakerRunner
runner: capabilities2_runner::SpeakerRunner
depends_on:
  "std_capabilities2/SpeakerLaunchRunner":
    provider: "std_capabilities2/SpeakerLaunchRunner"