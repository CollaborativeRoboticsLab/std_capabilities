# the empty provider
%YAML 1.1
---
name: WaypointRunner
spec_type: provider
spec_version: 1.1
description: "The capability provider for the follow_waypoint interface"
implements: std_capabilities/WaypointRunner
runner: capabilities2_runner::WayPointRunner
# depends_on:
#   "std_capabilities/Nav2LaunchRunner":
#     provider: "std_capabilities/Nav2LaunchRunner"
