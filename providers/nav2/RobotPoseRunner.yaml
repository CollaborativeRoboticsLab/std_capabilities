# the empty provider
%YAML 1.1
---
name: RobotPoseRunner
spec_type: provider
spec_version: 1.1
description: "The capability provider for the robot's odometry interface"
implements: std_capabilities2/RobotPoseRunner
runner: capabilities2_runner::RobotPoseRunner
depends_on:
  "std_capabilities2/Nav2LaunchRunner":
    provider: "std_capabilities2/Nav2LaunchRunner"