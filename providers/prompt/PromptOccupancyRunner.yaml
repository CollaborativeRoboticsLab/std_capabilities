# the empty provider
%YAML 1.1
---
name: PromptOccupancyRunner
spec_type: provider
spec_version: 1.1
description: "The capability provider for the prompting interface from prompt tools. This can be used for prompting the occupancy map around the robot."
implements: std_capabilities2/PromptOccupancyRunner
runner: capabilities2_runner::PromptOccupancyRunner
depends_on:
  "std_capabilities2/PromptLaunchRunner":
    provider: "std_capabilities2/PromptLaunchRunner"