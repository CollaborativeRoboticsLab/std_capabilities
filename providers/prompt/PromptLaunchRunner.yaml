# the base provider
%YAML 1.1
---
name: PromptLaunchRunner
spec_type: provider
spec_version: 1.1
description: "The capability provider for the prompt tools"
implements: std_capabilities2/PromptLaunchRunner
runner: prompt_bridge/prompt_bridge.launch.py
