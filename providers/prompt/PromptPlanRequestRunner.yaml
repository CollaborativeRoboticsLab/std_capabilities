# the empty provider
%YAML 1.1
---
name: PromptPlanRequestRunner
spec_type: provider
spec_version: 1.1
description: "The capability provider for the prompting for an execution plan. This can be used requesting a new plan at the beginning or when 
              new data is available"
implements: std_capabilities2/PromptPlanRequestRunner
runner: capabilities2_runner::PromptPlanRequestRunner
depends_on:
  "std_capabilities2/PromptLaunchRunner":
    provider: "std_capabilities2/PromptLaunchRunner"